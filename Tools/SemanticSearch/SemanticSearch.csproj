<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>SemanticSearch</AssemblyName>
    <RootNamespace>BS3D.Tools.SemanticSearch</RootNamespace>
  </PropertyGroup>

  <!--No project references and no MonoGame: this tool reads GitHub issues and the journal's Markdown and talks HTTP
      to LM Studio. It knows nothing of the game's types, so it builds and runs anywhere .NET does.-->
</Project>
